name: Auto-generate Alt Text

# Uses Google Gemini API to generate descriptive alt-text for images
# Rate limited to 5 requests per minute (free tier limit)
on:
  push:
    branches:
      - main
    paths:
      - '_posts/**'
      - 'assets/images/image posts/**'
  workflow_dispatch:  # Allow manual triggering

# Permissions needed to commit changes back
permissions:
  contents: write

jobs:
  generate-alt-text:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0  # Fetch full history for git operations
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r scripts/requirements.txt
      
      - name: Generate alt-text for images
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          python scripts/gemini_alt_text.py
        continue-on-error: true  # Don't fail the workflow if some images fail
      
      - name: Check for changes
        id: check-changes
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
          else
            echo "has_changes=false" >> $GITHUB_OUTPUT
          fi
      
      - name: Commit and push changes
        if: steps.check-changes.outputs.has_changes == 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add _posts/
          git commit -m "Auto-generate alt-text for images using Gemini API" || exit 0
          git push

